<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TechAppNew" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="appBackground">@drawable/bg_gradient_dark</item>
        <item name="appBackgroundAuth">@drawable/bg_gradient_dark</item>
        <item name="appText">@color/white</item>
    </style>
</resources>